<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>AviaMasters — Get Your Bonus</title>
  <style>
    :root{
      --bg1:#071028;
      --bg2:#0f1630;
      --gold1:#ffd24a;
      --gold2:#ff8c1a;
      --accent:#00ffd6;
      --card:#0f1116;
      --glass: rgba(255,255,255,0.04);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif;
      background: radial-gradient(1200px 500px at 10% 10%, rgba(255,200,0,0.03), transparent),
                  linear-gradient(180deg,var(--bg1),var(--bg2));
      color:#fff;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:24px;
    }

    .page {
      width:100%;
      max-width:820px;
      display:grid;
      grid-template-columns: 1fr 420px;
      gap:28px;
      align-items:center;
    }

    @media (max-width:900px){
      .page{grid-template-columns: 1fr; max-width:420px}
    }

    .hero {
      position:relative;
      border-radius:18px;
      overflow:hidden;
      background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(0,0,0,0.2));
      box-shadow: 0 10px 40px rgba(2,6,23,0.6);
      border:1px solid rgba(255,255,255,0.03);
    }

    .hero .image {
      width:100%;
      height:100%;
      min-height:300px;
      display:block;
      object-fit:cover;
      filter: drop-shadow(0 10px 30px rgba(0,0,0,0.6));
    }

    .overlay-top {
      position:absolute;
      inset:16px 16px auto 16px;
      display:flex;
      gap:12px;
      align-items:center;
      z-index:5;
    }

    .logo-pill{
      background:linear-gradient(90deg,var(--gold1),var(--gold2));
      color:#111;
      padding:8px 14px;
      border-radius:999px;
      font-weight:800;
      box-shadow:0 6px 30px rgba(255,170,0,0.12);
    }

    .badge{
      background:rgba(0,0,0,0.45);
      padding:6px 10px;
      border-radius:999px;
      font-size:13px;
      border:1px solid rgba(255,255,255,0.04);
      backdrop-filter: blur(6px);
    }

    .card {
      background: linear-gradient(180deg, rgba(20,20,20,0.9), rgba(10,10,10,0.85));
      border-radius:20px;
      padding:26px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.6);
      border:1px solid rgba(255,255,255,0.03);
      display:flex;
      flex-direction:column;
      gap:14px;
      align-items:stretch;
    }

    .title {
      font-size:22px;
      font-weight:800;
      color:var(--gold1);
      letter-spacing:0.3px;
    }

    .subtitle{
      font-size:13px;
      color:rgba(255,255,255,0.85);
      margin-top:-4px;
    }

    .features{
      margin:8px 0 6px 0;
      padding:0;
      list-style:none;
      display:grid;
      gap:8px;
    }

    .features li{
      display:flex;
      gap:10px;
      align-items:flex-start;
      font-size:14px;
      color:rgba(255,255,255,0.92);
    }

    .features li .dot{
      width:10px;
      height:10px;
      border-radius:50%;
      background:var(--accent);
      margin-top:6px;
      box-shadow:0 6px 16px rgba(0,255,214,0.08);
    }

    .cta {
      margin-top:8px;
      display:block;
      text-align:center;
      text-decoration:none;
      padding:14px;
      border-radius:12px;
      font-weight:900;
      font-size:17px;
      color:#081010;
      background: linear-gradient(90deg,var(--gold1),var(--gold2));
      box-shadow: 0 10px 30px rgba(255,170,0,0.18), inset 0 -3px 0 rgba(0,0,0,0.2);
      transition: transform .12s ease, box-shadow .12s ease;
    }
    .cta:active{transform:translateY(1px)}
    .cta:hover{box-shadow:0 14px 40px rgba(255,170,0,0.28)}

    .small {
      font-size:12px;
      color:rgba(255,255,255,0.65);
    }

    .hero::after{
      content:"";
      position:absolute;
      right:-80px;
      top:40%;
      width:400px;
      height:120px;
      background: radial-gradient(closest-side, rgba(255,200,0,0.06), transparent 60%);
      transform:rotate(-8deg);
      filter: blur(8px);
      z-index:2;
    }

    @keyframes floaty { 
      0%{transform:translateY(0)}
      50%{transform:translateY(-6px)}
      100%{transform:translateY(0)} 
    }
    .image{animation: floaty 6s ease-in-out infinite;}
  </style>
</head>
<body>

  <div class="page">

    <div class="hero">
      <div class="overlay-top">
        <div class="logo-pill">AVIA</div>
        <div class="badge">MASTERS</div>
      </div>

      <!-- IMPORTANT: save your image as "avia.jpg" in the same folder -->
      <img class="image" src="avia.jpg" alt="AviaMasters" />
    </div>

    <aside class="card">
      <div>
        <div class="title">AviaMasters — Exclusive Bonus</div>
        <div class="subtitle">Quick access to registration and the game</div>
      </div>

      <ul class="features">
        <li><span class="dot"></span> Instant registration</li>
        <li><span class="dot"></span> Russian language support</li>
        <li><span class="dot"></span> Mobile-friendly</li>
        <li><span class="dot"></span> Fast-paced gameplay</li>
      </ul>

      <!-- Your referral link -->
      <a class="cta" 
         href="https://vv-one96.com/v3/5651/landing-aviamasters-demo?p=z1rz" 
         target="_blank" rel="noopener noreferrer">
         🎮 Go to Game
      </a>

      <div class="small">Click the button to go directly to the game.</div>
    </aside>

  </div>

</body>
</html>
